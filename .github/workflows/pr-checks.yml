name: PR Checks

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  # ── Lint executor + Python examples ──────────────────────────
  validate-and-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -e ".[dev]" jsonschema
        working-directory: yaml-workflows/executor

      - name: Ruff lint — executor
        run: ruff check yaml-workflows/executor/orchestrio/

      - name: Ruff lint — Python examples
        run: ruff check python/

      - name: Ruff format check — executor
        run: ruff format --check yaml-workflows/executor/

      - name: Ruff format check — Python examples
        run: ruff format --check python/

      - name: Validate workflow YAML against schema
        run: |
          python - <<'SCRIPT'
          import json, sys, glob, yaml
          from jsonschema import validate, ValidationError

          with open("yaml-workflows/workflow-spec/v1/schema.json") as f:
              schema = json.load(f)

          files = sorted(
              glob.glob("yaml-workflows/examples/*.yaml")
              + glob.glob("yaml-workflows/workflows/*.yaml")
          )

          if not files:
              print("No YAML files found to validate")
              sys.exit(0)

          errors = 0
          for path in files:
              with open(path) as f:
                  doc = yaml.safe_load(f)
              try:
                  validate(instance=doc, schema=schema)
                  print(f"  OK  {path}")
              except ValidationError as e:
                  print(f"  FAIL  {path}: {e.message}")
                  errors += 1

          if errors:
              print(f"\n{errors} file(s) failed schema validation")
              sys.exit(1)
          else:
              print(f"\nAll {len(files)} file(s) passed")
          SCRIPT

  # ── Unit tests ────────────────────────────────────────────────
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -e ".[dev]"
        working-directory: yaml-workflows/executor

      - name: Run tests
        run: pytest yaml-workflows/executor/tests/ -v

  # ── Example README check ─────────────────────────────────────
  check-readmes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Verify example directories have READMEs
        run: |
          ERRORS=0
          for dir in python ansible terraform; do
            if [ ! -f "$dir/README.md" ]; then
              echo "::error::Missing README.md in $dir/"
              ERRORS=$((ERRORS + 1))
            else
              echo "  OK  $dir/README.md"
            fi
          done
          if [ "$ERRORS" -gt 0 ]; then
            echo "$ERRORS directory(ies) missing README.md"
            exit 1
          fi
